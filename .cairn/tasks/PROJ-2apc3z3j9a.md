---
id: PROJ-2apc3z3j9a
title: 'Mirror dbugd release: GoReleaser binary + install.sh + per-OS desktop jobs'
status: in_review
priority: high
checks:
  - desc: GoReleaser config valid + snapshot builds all 6 OS/arch binaries
    cmd: export PATH="$(go env GOPATH)/bin:$PATH"; goreleaser check && goreleaser build --snapshot --clean >/dev/null 2>&1 && [ "$(find dist -name cairn -o -name cairn.exe | wc -l)" -eq 6 ] && echo OK
    timeout: 420
    result: pass
  - desc: cairn version command works
    cmd: go run ./cmd/cairn version 2>&1 | grep -qi cairn && echo OK
    timeout: 120
    result: pass
  - desc: install.sh is POSIX-clean (sh -n; shellcheck if present)
    cmd: sh -n install.sh && (command -v shellcheck >/dev/null 2>&1 && shellcheck -s sh install.sh || true) && echo OK
    timeout: 60
    result: pass
  - desc: 'Manual: a dispatch/tag run produces binary archives + desktop installers on the release'
    type: manual
    result: pending
provenance:
  - {who: 'agent:claude', at: '2026-06-25T13:45:35Z', did: created}
  - {who: 'agent:claude', at: '2026-06-25T13:45:42Z', did: began session ses_cf010e935b1c4d2a14d9916c}
  - {who: 'agent:claude', at: '2026-06-25T13:50:21Z', did: updated}
  - {who: 'agent:claude', at: '2026-06-25T13:51:17Z', did: ran checks}
  - {id: n_srexyken, who: 'agent:claude', at: '2026-06-25T13:51:32Z', did: note, text: 'Mirrored dbugd''s release setup. New: .goreleaser.yaml (builds cairn for linux/darwin/windows × amd64/arm64, CGO off, ldflags -X main.version, before-hook builds the embedded web UI via pnpm + go mod tidy, tar.gz/zip archives + checksums.txt + attaches install.sh); install.sh (curl|sh — detect os/arch, download+verify archive from the GitHub release, install to BINDIR, run `cairn version`); cmd/cairn/main.go gained `var version="dev"` + `version`/`--version`/`-v` command. release.yml rewritten to dbugd''s shape: a GoReleaser `release` job (Go binary) + per-OS desktop-{macos,linux,windows} jobs (needs: release) that pnpm-install, set version, `pnpm tauri:build -- --bundles …`, generate checksums, and upload installers via softprops to the same release; TAURI_SIGNING_* passed for updater .sig; Apple signing scaffolded but guarded (if APPLE_CERTIFICATE != '''') so unsigned builds work now. Adapted from dbugd: pnpm (not npm), desktop/ (not web/) Tauri path. dist/ added to .gitignore. Docs: installation.md gained an "Install the binary" curl|sh section (placeholder until first tag); CHANGELOG updated. Verified locally with goreleaser v2.16.0: `goreleaser check` clean; snapshot built all 6 binaries with correct archs (PE32+ x86-64, Mach-O arm64, ELF x86-64); version injected (cairn 0.0.0-SNAPSHOT-…); install.sh sh -n clean; both YAMLs parse; docs build clean. Follow-ups: in-app updater latest.json is not generated by the new desktop jobs (dbugd doesn''t either) — auto-update no-ops until a feed is published; Apple/Windows code signing still needs secrets.'}
  - {who: 'agent:claude', at: '2026-06-25T13:51:50Z', did: finished session ses_cf010e935b1c4d2a14d9916c, text: "Mirrored dbugd's release setup for cairn — adding the standalone-binary distribution it was missing and restructuring the desktop release into per-OS jobs.\n\nNew:\n- .goreleaser.yaml — builds `cairn` for linux/darwin/windows × amd64/arm64 (CGO off), ldflags inject `-X main.version`, a before-hook builds the embedded web UI (pnpm) + `go mod tidy`, produces tar.gz/zip archives + checksums.txt, and attaches install.sh to the release.\n- install.sh — `curl -fsSL …/releases/latest/download/install.sh | sh`: detects OS/arch, downloads + checksum-verifies the archive, installs to BINDIR (default /usr/local/bin, sudo if needed), runs `cairn version`.\n- cmd/cairn/main.go — `var version = \"dev\"` + a `version`/`--version`/`-v` command (GoReleaser overrides version at build time).\n\nRewrote .github/workflows/release.yml to dbugd's shape (tags v*): a GoReleaser `release` job for the Go binary, then desktop-macos / desktop-linux / desktop-windows jobs (needs: release) that pnpm-install, stamp the version, run `pnpm tauri:build -- --bundles …`, checksum, and upload installers to the same release via softprops. TAURI_SIGNING_* is passed so updater .sig artifacts are signed; Apple signing/notarization is scaffolded but guarded (runs only when APPLE_CERTIFICATE is set) so unsigned builds work today. Adapted from dbugd: pnpm not npm, desktop/ not web/ for the Tauri project. dist/ gitignored.\n\nDocs: installation.md gained an \"Install the binary\" curl|sh section (with a placeholder note until the first tag); CHANGELOG [Unreleased] updated.\n\nVerified locally with goreleaser v2.16.0: `goreleaser check` clean; `goreleaser build --snapshot --clean` produced all six binaries with correct architectures (PE32+ x86-64, Mach-O arm64, ELF x86-64); version injection confirmed (`cairn 0.0.0-SNAPSHOT-…`); `cairn version` works; install.sh passes `sh -n`; both YAML files parse; docs build clean. All three command checks on the task pass.\n\nFollow-ups (manual check still open until observed in CI):\n- The in-app updater feed (latest.json) is NOT generated by the new desktop jobs — dbugd's doesn't either. Auto-update no-ops until a feed is published; the .sig artifacts are produced, so a small aggregation step could compose latest.json later.\n- Apple notarization + Windows code signing still need repo secrets; until then installers are unsigned and warn on first launch.\n- To ship: push a tag like v0.1.0 (requires TAURI_SIGNING_PRIVATE_KEY[_PASSWORD] secrets, already referenced). GoReleaser also needs no extra secret beyond GITHUB_TOKEN."}
assignee: agent:claude
active_attempt: att_cf010e935b1c4d2a14d9916c
---
Adopt the dbugd (`~/Projects/dbugd`) release setup. cairn currently has no standalone Go-binary distribution and no curl|sh installer.

## Add
- `cmd/cairn/main.go` — `var version = "dev"` + `version`/`--version` command (GoReleaser injects via `-ldflags -X main.version`).
- `.goreleaser.yaml` — build `cairn` for linux/darwin/windows × amd64/arm64 (CGO off), `before` hook builds the embedded web UI (pnpm), tar.gz/zip archives, checksums.txt, attach `install.sh`.
- `install.sh` — curl|sh installer (detect os/arch, download+verify archive from the GitHub release, install to BINDIR, run `cairn --version`).
- Rewrite `.github/workflows/release.yml` to dbugd's shape: a `release` GoReleaser job (Go binary) + `desktop-macos`/`desktop-linux`/`desktop-windows` jobs (pnpm + `desktop/`, `tauri build --bundles …`, upload installers + checksums via softprops). Keep TAURI updater signing; Apple signing optional/guarded.
- Docs/README install section: `curl … | sh` + archives, alongside the desktop app.

## Acceptance
- `cairn version` prints; `goreleaser check` passes; `goreleaser build --snapshot --clean` produces all 6 binaries locally; install.sh shellcheck-clean.